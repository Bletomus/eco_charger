name: eco_charger
description: A econet data recharge client designed to make recharging phones faster and easier

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hexcolor: ^2.0.4
  flutter_svg: ^0.22.0
  shared_preferences: ^2.0.7
  logger: ^1.1.0
  adaptive_theme: ^2.2.0
  riverpod: ^0.14.0+3
  flutter_phone_direct_caller: ^2.0.0
  cupertino_icons: ^1.0.2
  sim_data: ^0.0.2
  ussd_service: ^0.1.1
  permission_handler: ^8.1.6
  introduction_screen: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/icons/teacherLogo.svg
  uses-material-design: true

