import 'package:flutter/cupertino.dart';

class OneFusion extends StatelessWidget
{
  @override
  Widget build(BuildContext context)
  {
    return Container
    (
      child: Center
      (
        child: Text("Welcome to OneFusion"),
      ),
    );
  }

}