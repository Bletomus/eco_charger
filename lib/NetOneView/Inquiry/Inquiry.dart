import 'package:flutter/cupertino.dart';

class NetoneInquiry extends StatelessWidget
{
  @override
  Widget build(BuildContext context)
  {
    return Container
      (
      child: Center
        (
        child: Text("Welcome to Inquiry!"),
      ),
    );
  }

}