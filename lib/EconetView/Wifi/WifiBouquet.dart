import 'package:flutter/cupertino.dart';

/// WifiBouquet widget shows the following options when loaded
/// Widgets [kWifiBouquetSelectionWidgets]
///   WhatsAppBundles
class WifiBouquet extends StatelessWidget
{

  const WifiBouquet({Key? key}) : super(key:key);
  @override
  Widget build(BuildContext context)
  {
    //TODO Shared preference along with variables per menu
    throw UnimplementedError();
    return Container();
  }

}