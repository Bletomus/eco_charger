import 'package:flutter/cupertino.dart';

/// Account widget shows the following options when loaded
/// Widgets [kAccountSelectionWidgets]
///   WhatsAppBundles
class Account extends StatelessWidget
{

  const Account({Key? key}) : super(key:key);
  @override
  Widget build(BuildContext context)
  {
    //TODO Shared preference along with variables per menu
    throw UnimplementedError();
    return Container();
  }

}