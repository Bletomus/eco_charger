import 'package:flutter/cupertino.dart';

/// Learning widget shows the following options when loaded
/// Widgets [kLearningSelectionWidgets]
///   WhatsAppBundles
class Learning extends StatelessWidget
{

  const Learning({Key? key}) : super(key:key);
  @override
  Widget build(BuildContext context)
  {
    //TODO Shared preference along with variables per menu
    throw UnimplementedError();
    return Container();
  }

}