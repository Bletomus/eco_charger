import 'package:flutter/cupertino.dart';

/// Voice widget shows the following options when loaded
/// Widgets [kVoiceBundlesWidgets]
///   WhatsAppBundles
class Voice extends StatelessWidget
{

  const Voice({Key? key}) : super(key:key);
  @override
  Widget build(BuildContext context)
  {
    //TODO Shared preference along with variables per menu
    throw UnimplementedError();
    return Container();
  }

}