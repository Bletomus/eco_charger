import 'package:flutter/cupertino.dart';

/// SocialBouquet widget shows the following options when loaded
/// Widgets [kSocialBouquetSelectionWidgets]
///   WhatsAppBundles
class SocialBouquet extends StatelessWidget
{

  const SocialBouquet({Key? key}) : super(key:key);
  @override
  Widget build(BuildContext context)
  {
    //TODO Shared preference along with variables per menu
    throw UnimplementedError();
    return Container();
  }

}