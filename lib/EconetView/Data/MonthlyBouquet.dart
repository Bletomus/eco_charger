import 'package:flutter/cupertino.dart';

/// MonthlyBouquet widget shows the following options when loaded
/// Widgets [kMonthlyBouquetSelectionWidgets]
///   SelectedUser
class MonthlyBouquet extends StatelessWidget
{

  const MonthlyBouquet({Key? key}) : super(key:key);
  @override
  Widget build(BuildContext context)
  {
    //TODO Shared preference along with variables per menu
    throw UnimplementedError();
    return Container();
  }

}