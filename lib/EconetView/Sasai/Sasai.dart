import 'package:flutter/cupertino.dart';

/// SasaiBundles widget shows the following options when loaded
/// Widgets [kSasaiSelectionWidgets]
///   WhatsAppBundles
class SasaiBundles extends StatelessWidget
{

  const SasaiBundles({Key? key}) : super(key:key);
  @override
  Widget build(BuildContext context)
  {
    //TODO Shared preference along with variables per menu
    throw UnimplementedError();
    return Container();
  }

}